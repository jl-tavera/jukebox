{
  "name": "@jukebox/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "generate:wordmark": "bun run scripts/generate-wordmark.ts",
    "generate:help": "bun run scripts/generate-help.ts"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@jukebox/schema": "workspace:*",
    "citty": "^0.2.2",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
