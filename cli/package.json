{
  "name": "@jukebox/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:wordmark": "bun run scripts/check-wordmark.ts"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@jukebox/schema": "workspace:*",
    "citty": "^0.2.2",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
